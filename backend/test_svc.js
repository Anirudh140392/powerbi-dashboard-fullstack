import axios from 'axios';
import http from 'http';

async function test() {
    try {
        // Find out what port the server is really running on by checking lsof or netstat.
        // Or wait, let's just grep the actual port usage.
    } catch(e) {
        console.error(e.message);
    }
}
test();
